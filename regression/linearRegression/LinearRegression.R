# Gradient Descent Algorihm

# Loss Functions
# sum squared error
sumSquaredError <- function(theta, hypothesisCB) {
  
}

# Linear regression 
# this is h_theta(x)= = theta__0 + theta_1(x_1) + ... + theta_n(x_n)
# setting x_0 is at 1
linearRegression <-function(row, parameters) {
  target <- 1 + sum(row * parameters)
}

# another commet